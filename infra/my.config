[User Friendly name]
expr = arg1 == 11 && arg2 == 22
comment = Since comment option is given, this takes priority over "User Friendly name"
[And if comment options is not used, this becomes comment!]
expr = arg1 == 255
